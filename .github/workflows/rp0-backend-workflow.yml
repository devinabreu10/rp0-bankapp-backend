name: RP0 Banking Backend CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-rp0-bankapp-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Build and Run Tests with Maven
        run: ./mvnw clean package

      - name: Extract Project Version
        id: extract_version
        run: echo "VERSION=$(./mvnw -q -Dexec.executable='echo' -Dexec.args='${project.version}' --non-recursive exec:exec)" >> $GITHUB_OUTPUT

        # https://github.com/marketplace/actions/official-sonarqube-scan
#      - name: SonarQubeCloud Scan
#        uses: SonarSource/sonarqube-scan-action@v5.1.0
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#          SONAR_ROOT_CERT: ${{ secrets.SONAR_ROOT_CERT }}
#        with:
#          projectBaseDir: p0-bankapp-remake/src
#          args: >
#            -Dsonar.organization=${{ secrets.SONAR_ORG }}
#            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
#            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
#            -Dsonar.verbose=true

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Image to DockerHub
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ secrets.DOCKER_USERNAME }}/rp0-bankapp-api:${{ steps.extract_version.outputs.VERSION }}
          build-args: |
            PROFILE=dev
            APP_VERSION=${{ steps.extract_version.outputs.VERSION }}
